import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container:{
        flex:  1,
        backgroundColor: "#7da453",
        borderRadius:10,
        padding: 10
    },
    text:{
        color: "#fff",
        fontSize: 20,
    }
});
